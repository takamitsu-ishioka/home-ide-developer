{
  "theme": "dark",
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: Azure (referenced in CLAUDE.md for ALPHA/BETA/PROD Linux VMs; not corroborated by usage evidence)",
      "**Repository visibility**: PUBLIC — takamitsu-ishioka/home-ide-developer (github.com)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: .env / .env.<ENV> files (LOCAL|ALPHA|BETA|PROD), populated manually by the user from .env.template / .env.<ENV>.template; Coding Agent reads/writes only the .template files, never the real .env",
      "**Default / protected branches**: unknown — origin/HEAD unset; no rulesets or protected branches listed via gh for takamitsu-ishioka/home-ide-developer",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo (takamitsu-ishioka/home-ide-developer) and its origin remote only",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: freee (referenced in ~/.claude/CLAUDE.md notes on OAuth/time-clock quirks; config-derived, not usage-corroborated)",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: .claude/.credentials.json, bin/.env, bin/.env.example, bin/conflu/.env, bin/conflu/.env.example, bin/sensor_modules/.env.alpha, bin/sensor_modules/.env.beta, bin/sensor_modules/.env.local, bin/sensor_modules/.env.prod — credentials and environment secrets; share only with the repo owner (takamitsu-ishioka)",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (e.g. bin/sensor_modules/.env.prod, PROD environment per CLAUDE.md)",
      "**Protected deployment namespaces / environments**: PROD (per CLAUDE.md LOCAL|ALPHA|BETA|PROD environment naming) — fall back to the Sensitive remote targets heuristic otherwise",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development (personal home-directory tooling / dotfiles-as-IDE project)",
      "**Trusted repo**: takamitsu-ishioka/home-ide-developer (public, github.com:takamitsu-ishioka/home-ide-developer.git) — the working directory (~) and its origin remote; content ported or first read from outside this repo is not its own work, whoever directed the port",
      "**Org-specific CLIs**: None configured",
      "routine under takamitsu-ishioka/ prefix on github.com is the user's own trusted repo scope"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/bin/repo_name_sync.sh\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
